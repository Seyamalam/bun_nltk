{
  "name": "bun-nltk-wasm-browser-example",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prepare:wasm": "bun run scripts/copy-wasm.js",
    "dev": "bun run prepare:wasm && vite",
    "build": "bun run prepare:wasm && vite build",
    "preview": "vite preview",
    "deploy": "bun run build && wrangler deploy"
  },
  "dependencies": {
    "bun_nltk": "0.16.0"
  },
  "devDependencies": {
    "vite": "^7.1.3",
    "wrangler": "4.131.1"
  }
}
