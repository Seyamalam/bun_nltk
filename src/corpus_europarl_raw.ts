/** corpus.europarl_raw — shim (requires Europarl corpus data). */
export class EuroparlCorpusReader { constructor(..._a: unknown[]) { throw new Error("EuroparlCorpusReader requires nltk_data europarl_raw — not available in JS"); } }
